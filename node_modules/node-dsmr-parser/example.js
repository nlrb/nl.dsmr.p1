const parser   = require('./index');
const telegrams = ['example-telegram.txt', 'example-v4.txt', 'example-v3.txt'];

for (var i = 0; i < telegrams.length; i++) {
   console.log('>>>', telegrams[i]);
   var tel = require('fs').readFileSync(telegrams[i], 'utf-8');
   console.log(parser.validate(tel));
   console.log(parser.parse(tel).objects);
}
